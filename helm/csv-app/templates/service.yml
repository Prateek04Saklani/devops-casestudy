apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-csv-app
  namespace: {{ .Release.Namespace }}
spec:
  type: NodePort
  selector:
    app: {{ .Release.Name }}-csv-app
  ports:
    - port: 80
      targetPort: 80
